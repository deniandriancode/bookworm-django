*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

a, textarea, input {
  font-family: inherit;
  font-size: inherit;
  color: black;
  text-decoration: none;
}

body {
  display: flex;
}

/* BEGIN_sidebar */
#sidebar {
  border-right: 1px solid gray;
  min-height: 100vh;
  width: 150px;
  padding: .5em;
}
/* END_sidebar */

/* BEGIN_main */
main {
  min-height: 100vh;
  padding: 1em;
}

.add-button {
  width: 50px;
  height: 50px;
  border-radius: 100%;
  background: #1e90ff;
  position: fixed;
  bottom: 1em;
  right: 1em;
  box-shadow: 1px 1px 2px 0 #444;
}

.add-button a {
  display: inline-block;
  width: 50px;
  height: 50px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 2em;
}

#form-new-book div {
  display: grid;
  width: 600px;
}

#form-new-book div label {
  margin-top: 15px;
  margin-bottom: 5px;
}

#form-new-book input,
#form-new-book textarea {
  padding: .3em .7em;
  border-radius: .25em;
  border: 1px solid #bbb;
}

#form-new-book input:focus,
#form-new-book textarea:focus {
  outline: 1px solid #888;
}

#form-new-book textarea {
  min-height: 120px;
  resize: vertical;
}

#form-new-submit-button {
  margin-top: 2em;
  border: none;
  background-color: #1e90ff;
  box-shadow: 1px 1px 2px 0 #444;
  border-radius: .25em;
  padding: 1em 3em;
  color: white;
  cursor: pointer;
}

ul.book-list {
  list-style-type: none;
}
/* END_main */
